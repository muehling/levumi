<script>
    //Eintrag in der Navleiste markieren
    highlightNavItem('navbarSystem');
</script>

<% if @import_failure %>
  <div class="alert alert-danger" role="alert">
    Import fehlgeschlagen!
  </div>
<% end %>

<div class='container-fluid mt-3'>

  <!-- Tabs -->
  <ul class='nav nav-pills' id='user_tabs' role='tablist'>
    <li class='nav-item'>
      <a class='nav-link active' id='list-tab' data-toggle='tab' href='#list' role='tab' aria-controls='list' aria-selected='true'>Übersicht</a>
    </li>
    <li class='nav-item'>
      <a class='nav-link' id='new-tab' data-toggle='tab' href='#new' role='tab' aria-controls='new' aria-selected='false'>Neuer Test</a>
    </li>
  </ul>

  <!-- Tabinhalte -->
  <div class='tab-content mt-3'>
    <div class='tab-pane active' id='list' role='tabpanel' aria-labelledby='list-tab'>
      <div id='list_content'>
        <!-- Testtabelle -->
        <%= render 'table', export: false %>
      </div>
    </div>
    <div class='tab-pane' id='new' role='tabpanel' aria-labelledby='new-tab'>
      <div class='row'>
        <div class='col-4'>
          <div class='card card-body' id='new_content'>
            <!-- Test anlegen-->
            <%= render 'form', test: nil %>
          </div>
        </div>
      </div>
    </div>
  </div>

</div>
