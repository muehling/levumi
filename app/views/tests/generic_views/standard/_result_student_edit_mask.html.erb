<%result = local_assigns[:result]%>
<%= form_tag(user_group_assessment_measurement_results_path(@user, @group, @assessment, @measurement), :method => :put, class: "form", :data => {:remote => true}) do %>
    <% index = 0 %>
    <% result.items.each do |i| %>
        <% tempItem = Item.find(i) %>
        <% if tempItem.itemtype == 0 %>
            <div class="form-group">
              <p class="lead">
                <%= tempItem.shorthand %>
              </p>
              <div class="radio">
                <label>
                  <%= radio_button_tag "results[#{result.student.id}[#{i}]]", '1', result.responses[index] == 1 %>Richtig<br/>
                </label>
              </div>
              <div class="radio">
                <label>
                  <%= radio_button_tag "results[#{result.student.id}[#{i}]]", '0', result.responses[index] == 0 %>Falsch<br/>
                </label>
              </div>
              <div class="radio">
                <label>
                  <%= radio_button_tag "results[#{result.student.id}[#{i}]]", 'NA', result.responses[index] == nil %><i>Nicht bearbeitet</i><br/>
                </label>
              </div>
            </div>
            <br/>
        <% end %>
        <% index = index + 1 %>
    <%end%>
    <%= submit_tag 'Speichern', :class => "btn btn-primary" %>
    <%= link_to "Zurück",  [@user, @group, @assessment], :data => {:remote => true}%>
<% end %>