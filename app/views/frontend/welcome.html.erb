<div class="jumbotron">
  <h1>LeVuMi</h1>
  <h3>Zugang für Schüler/-innen</h3>
  <br/>
  <%= form_tag(url_for(:controller => 'frontend', :action => 'login'), :class => "form") do %>
      <p>
        Hier bitte Zugangscode eingeben:
      </p>
      <div class="form-group">
        <label class="sr-only" for="login">Code</label>
        <%= text_field_tag "login", nil, type: "text", class: "form-control", placeholder: "Code"%>
      </div>
      <!-- Passwort schonmal als Feld hinzugefügt, ohne Funktion-->
      <p>
        Hier bitte Passwort eingeben:
      </p>
      <div class="form-group">
        <label class="sr-only" for="login">Passwort</label>
        <%= text_field_tag "password", nil, type: "text", class: "form-control", placeholder: "Passwort"%>
      </div>
      <%= submit_tag 'Einloggen', :class => "btn btn-primary" %>
  <% end %>
</div>
