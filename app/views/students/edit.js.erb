$('#studentForm').html("<%= escape_javascript(render 'form') %>");
try{
    $('#student_name').val(sjcl.decrypt(sessionStorage['encryptKey'], $('#student_name').val()));
}
catch (e){
    $('#student_name').val("Kind<%=@student.id%>" )
}