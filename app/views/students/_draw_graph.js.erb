var chart<%=test%> = c3.generate({
    bindto: '#chart<%=test%>',
    data: {
        x: 'x',
        columns: [
            ['x',
                <% results.each do |r| %>
                '<%= r.measurement.date.to_date %>',
                <% end %>
            ],
            ['<%= student.name %>',
                <% results.each do |r| %>
                <% unless r.nil? %>
                <%= r.score.nil? ? "null" : r.score %>
                <% else %>
                null
                <% end %>,
                <% end %>
            ],
            ['Maximum',
                <% results.each do |r| %>
                <% unless r.nil? %>
                <%= r.measurement.max.nil? ? "null" : r.measurement.max %>
                <% else %>
                null
                <% end %>,
                <% end %>
            ],
            ['Minimum',
                <% results.each do |r| %>
                <% unless r.nil? %>
                <%= r.measurement.min.nil? ? "null" : r.measurement.min %>
                <% else %>
                null
                <% end %>,
                <% end %>
            ],
            ['Durchschnitt',
                <% results.each do |r| %>
                <% unless r.nil? %>
                <%= r.measurement.average.nil? ? "null" : r.measurement.average %>
                <% else %>
                null
                <% end %>,
                <% end %>
            ]
        ],
    },
    color: {
        pattern: ['#46B8DA', '#5CB85C', '#D9534F', '#F0AD4E']
    },
    axis: {
        x: {
            type: 'timeseries',
            tick: {
                fit: true,
                format: "%e %b %y"
            },
            label: {
                text: 'Testzeitpunkte',
                position: 'outer-left'
            }
        },
        y: {
            label: {
                text: 'Anzahl richtiger Items',
                position: 'outer-middle'
            },
            min: 1
        }
    },
    line: {
        connectNull: true
    },
    legend: {
        hide: false
    },
    tooltip: {
        contents: function (d, defaultTitleFormat, defaultValueFormat, color) {
            var res = "<table class='table table-condensed table-striped' style='background-color:white'>";
            res = res + "<tr><td>Schüler/-in</td><td>" + d[0].name + "</td></tr>";
            res = res + "<tr><td>Punkte</td><td>" + d[0].value + "</td></tr>";
            res = res + "<tr><td>Minimum</td><td>" + Math.min(...chart<%=test%>.data.values('<%= student.name %>')) + "</td></tr>";
            res = res + "<tr><td>Maximum</td><td>" + Math.max(...chart<%=test%>.data.values('<%= student.name %>')) + "</td></tr>";
            res = res + "</table>";
            return res;
        }
    }
});
chart<%=test%>.focus(['<%= student.name %>']);