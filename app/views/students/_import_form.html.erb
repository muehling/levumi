<% unless alert.blank? %>
    <div class="alert alert-danger">
        <%= alert %>
    </div>
<% end %>

<%= form_for([@user, @group, @student], :html => {:class => "form-inline"}, multipart: true) do |f| %>
    <div class="form-group">
      <%= f.label "Datei", :class => "sr-only" %>
      <%= file_field_tag "file" %>
    </div>

  <div class="form-group">
    <%= f.submit "Hochladen", :class => "btn btn-primary" %>
  </div>
<% end %>
