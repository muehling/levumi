<%
  data = {
      groups: @groups,
      group_info: @groups.each.map{|g| g.get_home_info}
  }
%>

<script>
    //Eintrag in der Navleiste markieren
    highlightNavItem("navbarHome");
</script>

<div id='levumi' data="<%= data.to_json%>">

  <!-- Spinner -->
  <div v-if="!$data">
    <b-row>
      <div class="spinner">
        <div class="bounce1"></div>
        <div class="bounce2"></div>
        <div class="bounce3"></div>
      </div>
    </b-row>
  </div>

  <home-app></home-app>
</div>
