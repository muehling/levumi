<!DOCTYPE html>
<html>
  <head>
  <title>Levumi</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_javascript_tag 'application' %>
    <%= vite_stylesheet_tag 'application' %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= yield :head if content_for? :head %>
    <%= vite_client_tag %>
  </head>

  <body>
    <div class='container'>
      <div class="position-absolute" style="top: 1em; right: 2em;">
        <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#admin-login" aria-expanded="false" aria-controls="admin-login">
          Admin-Zugang
        </button>
      </div>
      <div class='card position-absolute' style='top: 3em; right: 2em;'>
        <div class="collapse" id="admin-login">
          <%= form_tag url_for(controller: 'application', action: 'login'), class: 'px-4 py-3 mx-3 mx-md-0 mw-100', onsubmit: "sessionStorage['login'] = $('#password').val();" do%>
            <div class='form-group'>
              <input type='email' name='email' class='form-control <%= @retry ? 'is-invalid':'' %>' id='email' placeholder='E-Mail Adresse'>
            </div>
            <div class='form-group'>
              <input type='password' name='password' class='form-control <%= @retry ? 'is-invalid':'' %>' id='password' placeholder='Passwort'>
              <div class='invalid-feedback'>
                Benutzername oder Passwort ist falsch!
              </div>
            </div>
            <%= submit_tag 'Einloggen', class: 'btn btn-primary'%>
          <% end %>
        </div>
      </div>
      <div class="text-center mt-4">
        <img src='/images/shared/Levumi-normal.jpg' alt='Levumi' width='50%' height='50%'/>
        <p class="mt-3" style="font-weight: bold; font-size: 2em;">Levumi wird gerade gewartet.<br> Wir sind in Kürze wieder für sie da!</p>
      </div>     
  </body>
</html>